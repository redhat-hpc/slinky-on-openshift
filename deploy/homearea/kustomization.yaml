resources:
  - pvc.yaml
  - deployment.yaml

labels:
  - pairs:
      app.kubernetes.io/name: slurm
      app.kubernetes.io/component: homearea
    includeSelectors: true
    includeTemplates: true

namespace: slurm
